# GForce Mission Support — Project Context

## My role in the system  ⟨federation card⟩
- **I am:** gforce-mission-support — an app/tool sub-project under **GForce**.
- **Node type:** Sub-project (deliverable). My **parent business unit is
  `~/Projects/GFORCE-Space/`** — strategy, brand, and roadmap live there.
- **Orchestrator = `~/finance`** — the command center; holds cross-domain truth and
  knows every node via `system-map.md`. I rarely talk to it directly — I report
  through my parent.
- **I pull building blocks, never copy:** `~/skills` (methods), `~/tools` (mechanisms).

### Two-way protocol
- **⬆ Up:** anything cross-domain (money, scope, a decision GForce must make) goes to
  my `BOARD.md` under **⬆ Flags → orchestrator** (the weekly recon sweeps it) — and I
  flag my parent `~/Projects/GFORCE-Space/BOARD.md` for roadmap-level items.
- **⬇ Down:** tasks arrive via my `BOARD.md` under **⬇ From orchestrator** (or relayed
  by my parent).
- **Rule:** I'm a build artifact. Strategy lives in GForce; capital lives in `~/finance`.

## Project context
See `README.md`. This is a built app (AI Studio); deploy/run notes live in the repo.
